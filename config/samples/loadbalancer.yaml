apiVersion: v1
kind: Service
metadata:
  name: example-service
  annotations:
    network.schiff.telekom.de/pool: ipaddresspool-sample
spec:
  selector:
    app: example
  ports:
    - port: 8765
      targetPort: 9376
  type: LoadBalancer